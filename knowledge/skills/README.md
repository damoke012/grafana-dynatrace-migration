# skills/

Reusable, environment-agnostic playbooks.

- `migrate-dashboard.md`        — end-to-end: export -> audit -> translate -> deploy
- `promql-to-dql.md`            — PromQL -> DQL translation patterns
- `build-healthcheck-rollup.md` — the aggregated OK / red-count view
