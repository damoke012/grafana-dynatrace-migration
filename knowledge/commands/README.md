# commands/

Generic, copy-and-modify command templates. Set the env vars locally; they
never live in git.

- `grafana.md`   — list / export / inspect Grafana dashboards
- `dynatrace.md` — metric existence check, DQL, Documents API
- `monaco.md`    — deploy dashboards as config-as-code
